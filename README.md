# Cli-Expense-Tracker
This project is a simple command-line tool which allows the user to track their expenses. It will include date, category, cost and this data will be written to a CSV database The program will feature a CLI menu which allows the user to either add a new record or read the existing records.
